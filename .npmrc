# 1) default: the public registry for everything
registry=https://registry.npmjs.org/

# 2) your scope goes to GitHub Packages
@deimos308:registry=https://npm.pkg.github.com/
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}

# 3) auth - keep the token dynamic so it can come from the secret
always-auth=true

# 4) CI-only tweaks
git-checks=false            # skip the clean-tree check in the runner
access=restricted           # publish as a private package
tag=latest                  # always publish to the latest tag